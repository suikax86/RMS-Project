<UserControl x:Class="RMS_Project.View.UserControls.MyTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="250">
    <Grid>
        <Grid Background="White">
            <TextBox x:Name="txtInput" VerticalContentAlignment="Center" FontSize="16" FontWeight="Light"
                     Panel.ZIndex="0" Background="Transparent" Height="40" TextChanged="txtInput_TextChanged" />
            <TextBlock x:Name="txtPlaceHoder" Text="{Binding PlaceHolder}" FontSize="16" FontWeight="Light"
                       VerticalAlignment="Center" Margin="5,0,0,0" Panel.ZIndex="-1" />
            <Button x:Name="btnClear" Content="x" HorizontalAlignment="Right" Width="30" Height="40"
                    Background="Transparent" Foreground="LightGray" BorderThickness="0" Click="btnClear_Click" />
        </Grid>
    </Grid>
</UserControl>